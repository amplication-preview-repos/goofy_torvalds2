apiVersion: v1
kind: Service
metadata:
  name: {{ include "car-auction.fullname" . }}
  labels:
    {{- include "car-auction.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "car-auction.selectorLabels" . | nindent 4 }}
